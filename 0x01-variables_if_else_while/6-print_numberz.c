#include <stdio.h>
/**
 * main - Entry point
 * This program prints nos from 0 to 10
 * Return: Always 0 (success/corect)
 */

int main(void)
{
	int num;
	for (num = 48 ; num <= 57; num++)
	{
		putchar(num);
	}
	putchar ('\n');
	return (0);
}
